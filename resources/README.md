### Put all resouce files here with sprecific names. For example:
- `re-cli.jar` - Revanced CLI JAR file
- `patches.rvp` - Revanced Patches file
- `revanced.keystore` - Keystore file for signing patched APKs
- Original APK files (e.g., `yt.apk`, `yt-music.apk`, `photos.apk`)